{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <section class="jumbotron jumbotron-fluid text-center">
        <img src="{{ asset('images/logo/TerrePlurielle.png') }}" class="d-inline-block align-top" alt="">
    </section>

    <section class="container">

        <article class="row d-fex justify-content-center">
            <h4>Editer votre catégorie</h4>
        </article>

        <article>
            {{ form_start(form)}}
                {{form_rest(form)}}
                <button class="btn btn-success">editer</button>
                <a href="{{ path('admin_categorie')}}" class="btn btn-dark">retour</a>
            {{ form_end(form) }}
            
        </article>
        <div id="filename"></div>
    </section>
{% endblock %}

{% block javascripts %}
        <script>
            $('#categorie_imageFile').on('change',function(){
                //get the file name
                var fileName = $(this).val();
                //replace the "Choose a file" label
                $(this).next('.custom-file-label').html(fileName);
            })
        </script>
{% endblock %}
