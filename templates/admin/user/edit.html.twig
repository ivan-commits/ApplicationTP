{% extends 'admin/index.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <section id="header" class="jumbotron jumbotron-fluid text-center">
        <img src="{{ asset('images/logo/TerrePlurielle.png') }}" class="d-inline-block align-top" alt="">
    </section>

    <section class="container admin">

        <article class="container-content edit-user">

            <div class="title-content">
                <h1>Editer utilisateur</h1>
            </div>

 
            {{ form_start(form)}}
                {{form_rest(form)}}
            <div class="navbot-admin">
                <button class="btn">editer</button>
                <a class="btn" href="{{ path('admin_user')}}">retour</a>
            </div>
            {{ form_end(form) }}

    </section>
{% endblock %}