{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <section class="jumbotron jumbotron-fluid text-center">
        <img src="{{ asset('images/logo/TerrePlurielle.png') }}" class="d-inline-block align-top" alt="">
    </section>

    <section class="container mb-4">

        <article class="row d-fex justify-content-center">
            <h4>Créer votre utilisateur</h4>
        </article>

        <article>
            {{ form_start(form)}}
                {{form_rest(form)}}
                <button class="btn btn-success">créer</button>
                <a href="{{ path('admin_user')}}" class="btn btn-dark">retour</a>
            {{ form_end(form) }}
        </article>
    </section>
{% endblock %}