{% extends 'security/index.html.twig' %}
{% block body %}
    <section class="jumbotron jumbotron-fluid text-center">
        <img src="{{ asset('images/logo/TerrePlurielle.png') }}" class="d-inline-block align-top" alt="">
        <hr>
        <p class="lead">inscription</p>
    </section>

   <section class="container connexion mb-4 ">

        <div class="form-signin">
            {{ form_start(registrationForm) }}
            {{ form_row(registrationForm.firstName , {'attr':{'placeholder':'Prénom'}}) }}
            {{ form_row(registrationForm.surname,{'attr':{'placeholder':'Nom'}}) }}
            {{ form_row(registrationForm.email, {'attr':{'placeholder':'Adresse e-mail'}}) }}
            {{ form_row(registrationForm.password, {'label':'Mot de passe','attr':{'placeholder':'Mot de passe'}}) }}
            {{ form_row(registrationForm.confirm_password, {'label':'Confirmation mot de passe','attr':{'placeholder':'Confirmation mot de passe'}}) }}
            <div class="row  d-flex justify-content-around">
                <button class="btn btn-md btn-dark btn_block" type="submit">Inscription</button>
                <a class="btn btn-outline-light btn-md" href="{{ path('app_login') }}">Retour</a>
            </div>
            {{ form_end(registrationForm) }}
        </div>


    </section>
{% endblock %}